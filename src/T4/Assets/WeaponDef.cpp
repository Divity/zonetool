#include "stdafx.hpp"
#include "../IW5/Assets/WeaponDef.hpp"

namespace ZoneTool
{
	namespace T4
	{
		void IWeaponDef::dump(WeaponDef* asset, ZoneMemory* mem)
		{
			if (!asset || !asset->szInternalName)
			{
				return;
			}

			auto* complete = mem->Alloc<IW5::WeaponCompleteDef>();
			auto* def = mem->Alloc<IW5::WeaponDef>();

			complete->weapDef = def;

			complete->szInternalName = reinterpret_cast<const char*>(asset->szInternalName);
			complete->szDisplayName = reinterpret_cast<const char*>(asset->szDisplayName);
			def->szOverlayName = reinterpret_cast<const char*>(asset->szOverlayName);
			def->gunXModel = mem->Alloc<IW5::XModel*>(16);
			for (auto i = 0; i < 16; i++) def->gunXModel[i] = reinterpret_cast<IW5::XModel*>(asset->gunXModel[i]);
			def->handXModel = reinterpret_cast<IW5::XModel*>(asset->handXModel);
			complete->szXAnims = mem->Alloc<const char*>(42);
			for (auto i = 0; i < 35; i++) complete->szXAnims[i] = reinterpret_cast<const char*>(asset->szXAnims[i]);
			def->szModeName = reinterpret_cast<const char*>(asset->szModeName);
			complete->hideTags = mem->Alloc<unsigned __int16>(32);
			for (auto i = 0; i < 8; i++) complete->hideTags[i] = asset->hideTags[i];
			def->notetrackSoundMapKeys = mem->Alloc<__int16>(24);
			for (auto i = 0; i < 20; i++) def->notetrackSoundMapKeys[i] = asset->notetrackSoundMapKeys[i];
			def->notetrackSoundMapValues = mem->Alloc<__int16>(24);
			for (auto i = 0; i < 20; i++) def->notetrackSoundMapValues[i] = asset->notetrackSoundMapValues[i];
			def->playerAnimType = asset->playerAnimType;
			def->weapType = static_cast<int>(asset->weapType);
			def->weapClass = static_cast<int>(asset->weapClass);
			def->penetrateType = static_cast<int>(asset->penetrateType);
			complete->impactType = static_cast<int>(asset->impactType);
			def->inventoryType = static_cast<int>(asset->inventoryType);
			def->fireType = static_cast<int>(asset->fireType);
			def->offhandClass = static_cast<int>(asset->offhandClass);
			def->stance = static_cast<int>(asset->stance);
			def->viewFlashEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->viewFlashEffect);
			def->worldFlashEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->worldFlashEffect);
			def->pickupSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->pickupSound.sound);
			def->pickupSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->pickupSoundPlayer.sound);
			def->ammoPickupSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->ammoPickupSound.sound);
			def->ammoPickupSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->ammoPickupSoundPlayer.sound);
			def->projectileSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->projectileSound.sound);
			def->pullbackSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->pullbackSound.sound);
			def->pullbackSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->pullbackSoundPlayer.sound);
			def->fireSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireSound.sound);
			def->fireSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireSoundPlayer.sound);
			def->fireLoopSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireLoopSound.sound);
			def->fireLoopSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireLoopSoundPlayer.sound);
			def->fireStopSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireStopSound.sound);
			def->fireStopSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireStopSoundPlayer.sound);
			def->fireLastSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireLastSound.sound);
			def->fireLastSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->fireLastSoundPlayer.sound);
			def->emptyFireSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->emptyFireSound.sound);
			def->emptyFireSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->emptyFireSoundPlayer.sound);
			def->meleeSwipeSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->meleeSwipeSound.sound);
			def->meleeSwipeSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->meleeSwipeSoundPlayer.sound);
			def->meleeHitSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->meleeHitSound.sound);
			def->meleeMissSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->meleeMissSound.sound);
			def->rechamberSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->rechamberSound.sound);
			def->rechamberSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->rechamberSoundPlayer.sound);
			def->reloadSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadSound.sound);
			def->reloadSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadSoundPlayer.sound);
			def->reloadEmptySound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadEmptySound.sound);
			def->reloadEmptySoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadEmptySoundPlayer.sound);
			def->reloadStartSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadStartSound.sound);
			def->reloadStartSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadStartSoundPlayer.sound);
			def->reloadEndSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadEndSound.sound);
			def->reloadEndSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->reloadEndSoundPlayer.sound);
			def->detonateSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->detonateSound.sound);
			def->detonateSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->detonateSoundPlayer.sound);
			def->nightVisionWearSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->nightVisionWearSound.sound);
			def->nightVisionWearSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->nightVisionWearSoundPlayer.sound);
			def->nightVisionRemoveSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->nightVisionRemoveSound.sound);
			def->nightVisionRemoveSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->nightVisionRemoveSoundPlayer.sound);
			def->altSwitchSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->altSwitchSound.sound);
			def->altSwitchSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->altSwitchSoundPlayer.sound);
			def->raiseSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->raiseSound.sound);
			def->raiseSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->raiseSoundPlayer.sound);
			def->firstRaiseSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->firstRaiseSound.sound);
			def->firstRaiseSoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->firstRaiseSoundPlayer.sound);
			def->putawaySound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->putawaySound.sound);
			def->putawaySoundPlayer = reinterpret_cast<IW5::snd_alias_list_t*>(asset->putawaySoundPlayer.sound);
			def->bounceSound = reinterpret_cast<IW5::snd_alias_list_t**>(asset->bounceSound);
			def->viewShellEjectEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->viewShellEjectEffect);
			def->worldShellEjectEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->worldShellEjectEffect);
			def->viewLastShotEjectEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->viewLastShotEjectEffect);
			def->worldLastShotEjectEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->worldLastShotEjectEffect);
			def->reticleCenter = reinterpret_cast<IW5::Material*>(asset->reticleCenter);
			def->reticleSide = reinterpret_cast<IW5::Material*>(asset->reticleSide);
			def->iReticleCenterSize = asset->iReticleCenterSize;
			def->iReticleSideSize = asset->iReticleSideSize;
			def->iReticleMinOfs = asset->iReticleMinOfs;
			def->activeReticleType = static_cast<IW5::activeReticleType_t>(asset->activeReticleType);
			memcpy(def->vStandMove, asset->vStandMove, sizeof def->vStandMove);
			memcpy(def->vStandRot, asset->vStandRot, sizeof def->vStandRot);
			memcpy(def->vDuckedOfs, asset->vDuckedOfs, sizeof def->vDuckedOfs);
			memcpy(def->vDuckedMove, asset->vDuckedMove, sizeof def->vDuckedMove);
			memcpy(def->vDuckedRot, asset->vDuckedRot, sizeof def->vDuckedRot);
			memcpy(def->vProneOfs, asset->vProneOfs, sizeof def->vProneOfs);
			memcpy(def->vProneMove, asset->vProneMove, sizeof def->vProneMove);
			memcpy(def->vProneRot, asset->vProneRot, sizeof def->vProneRot);
			def->fPosMoveRate = asset->fPosMoveRate;
			def->fPosProneMoveRate = asset->fPosProneMoveRate;
			def->fStandMoveMinSpeed = asset->fStandMoveMinSpeed;
			def->fDuckedMoveMinSpeed = asset->fDuckedMoveMinSpeed;
			def->fProneMoveMinSpeed = asset->fProneMoveMinSpeed;
			def->fPosRotRate = asset->fPosRotRate;
			def->fPosProneRotRate = asset->fPosProneRotRate;
			def->fStandRotMinSpeed = asset->fStandRotMinSpeed;
			def->fDuckedRotMinSpeed = asset->fDuckedRotMinSpeed;
			def->fProneRotMinSpeed = asset->fProneRotMinSpeed;
			def->worldClipModel = reinterpret_cast<IW5::XModel*>(asset->worldClipModel);
			def->rocketModel = reinterpret_cast<IW5::XModel*>(asset->rocketModel);
			def->knifeModel = reinterpret_cast<IW5::XModel*>(asset->knifeModel);
			def->worldKnifeModel = reinterpret_cast<IW5::XModel*>(asset->worldKnifeModel);
			def->hudIcon = reinterpret_cast<IW5::Material*>(asset->hudIcon);
			def->hudIconRatio = static_cast<IW5::weaponIconRatioType_t>(asset->hudIconRatio);
			def->ammoCounterIcon = reinterpret_cast<IW5::Material*>(asset->ammoCounterIcon);
			def->ammoCounterIconRatio = static_cast<IW5::weaponIconRatioType_t>(asset->ammoCounterIconRatio);
			def->ammoCounterClip = static_cast<IW5::ammoCounterClipType_t>(asset->ammoCounterClip);
			def->iStartAmmo = asset->iStartAmmo;
			def->szAmmoName = reinterpret_cast<const char*>(asset->szAmmoName);
			def->iAmmoIndex = asset->iAmmoIndex;
			def->szClipName = reinterpret_cast<const char*>(asset->szClipName);
			def->iClipIndex = asset->iClipIndex;
			def->iMaxAmmo = asset->iMaxAmmo;
			complete->iClipSize = asset->iClipSize;
			def->shotCount = asset->shotCount;
			def->szSharedAmmoCapName = reinterpret_cast<const char*>(asset->szSharedAmmoCapName);
			def->iSharedAmmoCapIndex = asset->iSharedAmmoCapIndex;
			def->iSharedAmmoCap = asset->iSharedAmmoCap;
			def->damage = asset->damage;
			def->playerDamage = asset->playerDamage;
			def->iMeleeDamage = asset->iMeleeDamage;
			def->iDamageType = asset->iDamageType;
			complete->iFireTime = asset->iFireTime;
			complete->iAltRaiseTime = asset->iAltRaiseTime;
			complete->iFirstRaiseTime = asset->iFirstRaiseTime;
			def->requireLockonToFire = asset->requireLockonToFire != 0;
			def->noAdsWhenMagEmpty = asset->noAdsWhenMagEmpty != 0;
			def->avoidDropCleanup = asset->avoidDropCleanup != 0;
			def->autoAimRange = asset->autoAimRange;
			def->aimAssistRange = asset->aimAssistRange;
			def->aimAssistRangeAds = asset->aimAssistRangeAds;
			def->aimPadding = asset->aimPadding;
			def->enemyCrosshairRange = asset->enemyCrosshairRange;
			def->crosshairColorChange = asset->crosshairColorChange != 0;
			def->moveSpeedScale = asset->moveSpeedScale;
			def->adsMoveSpeedScale = asset->adsMoveSpeedScale;
			def->sprintDurationScale = asset->sprintDurationScale;
			complete->fAdsZoomFov = asset->fAdsZoomFov;
			def->fAdsZoomInFrac = asset->fAdsZoomInFrac;
			def->fAdsZoomOutFrac = asset->fAdsZoomOutFrac;
			def->overlayInterface = static_cast<int>(asset->overlayInterface);
			def->fAdsBobFactor = asset->fAdsBobFactor;
			def->fAdsViewBobMult = asset->fAdsViewBobMult;
			def->fHipSpreadStandMin = asset->fHipSpreadStandMin;
			def->fHipSpreadDuckedMin = asset->fHipSpreadDuckedMin;
			def->fHipSpreadProneMin = asset->fHipSpreadProneMin;
			def->hipSpreadStandMax = asset->hipSpreadStandMax;
			def->hipSpreadDuckedMax = asset->hipSpreadDuckedMax;
			def->hipSpreadProneMax = asset->hipSpreadProneMax;
			def->fHipSpreadDecayRate = asset->fHipSpreadDecayRate;
			def->fHipSpreadFireAdd = asset->fHipSpreadFireAdd;
			def->fHipSpreadTurnAdd = asset->fHipSpreadTurnAdd;
			def->fHipSpreadMoveAdd = asset->fHipSpreadMoveAdd;
			def->fHipSpreadDuckedDecay = asset->fHipSpreadDuckedDecay;
			def->fHipSpreadProneDecay = asset->fHipSpreadProneDecay;
			def->fHipReticleSidePos = asset->fHipReticleSidePos;
			complete->iAdsTransInTime = asset->iAdsTransInTime;
			complete->iAdsTransOutTime = asset->iAdsTransOutTime;
			def->fAdsIdleAmount = asset->fAdsIdleAmount;
			def->fHipIdleAmount = asset->fHipIdleAmount;
			def->adsIdleSpeed = asset->adsIdleSpeed;
			def->hipIdleSpeed = asset->hipIdleSpeed;
			def->fIdleCrouchFactor = asset->fIdleCrouchFactor;
			def->fIdleProneFactor = asset->fIdleProneFactor;
			def->fGunMaxPitch = asset->fGunMaxPitch;
			def->fGunMaxYaw = asset->fGunMaxYaw;
			def->swayMaxAngle = asset->swayMaxAngle;
			def->swayLerpSpeed = asset->swayLerpSpeed;
			def->swayPitchScale = asset->swayPitchScale;
			def->swayYawScale = asset->swayYawScale;
			def->swayHorizScale = asset->swayHorizScale;
			def->swayVertScale = asset->swayVertScale;
			def->swayShellShockScale = asset->swayShellShockScale;
			def->adsSwayMaxAngle = asset->adsSwayMaxAngle;
			def->adsSwayLerpSpeed = asset->adsSwayLerpSpeed;
			def->adsSwayPitchScale = asset->adsSwayPitchScale;
			def->adsSwayYawScale = asset->adsSwayYawScale;
			def->adsSwayHorizScale = asset->adsSwayHorizScale;
			def->adsSwayVertScale = asset->adsSwayVertScale;
			def->bRifleBullet = asset->bRifleBullet != 0;
			def->armorPiercing = asset->armorPiercing != 0;
			def->bBoltAction = asset->bBoltAction != 0;
			def->aimDownSight = asset->aimDownSight != 0;
			def->bRechamberWhileAds = asset->bRechamberWhileAds != 0;
			def->adsViewErrorMin = asset->adsViewErrorMin;
			def->adsViewErrorMax = asset->adsViewErrorMax;
			def->bCookOffHold = asset->bCookOffHold != 0;
			def->bClipOnly = asset->bClipOnly != 0;
			def->adsFireOnly = asset->adsFireOnly != 0;
			def->cancelAutoHolsterWhenEmpty = asset->cancelAutoHolsterWhenEmpty != 0;
			def->suppressAmmoReserveDisplay = asset->suppressAmmoReserveDisplay != 0;
			complete->enhanced = asset->enhanced != 0;
			def->laserSightDuringNightvision = asset->laserSightDuringNightvision != 0;
			complete->killIcon = reinterpret_cast<IW5::Material*>(asset->killIcon);
			def->killIconRatio = static_cast<IW5::weaponIconRatioType_t>(asset->killIconRatio);
			def->flipKillIcon = asset->flipKillIcon != 0;
			complete->dpadIcon = reinterpret_cast<IW5::Material*>(asset->dpadIcon);
			complete->dpadIconRatio = static_cast<IW5::weaponIconRatioType_t>(asset->dpadIconRatio);
			def->bNoPartialReload = asset->bNoPartialReload != 0;
			def->bSegmentedReload = asset->bSegmentedReload != 0;
			def->iReloadAmmoAdd = asset->iReloadAmmoAdd;
			def->iReloadStartAdd = asset->iReloadStartAdd;
			complete->szAltWeaponName = reinterpret_cast<const char*>(asset->szAltWeaponName);
			def->blocksProne = asset->blocksProne != 0;
			def->silenced = asset->silenced != 0;
			def->iExplosionRadius = asset->iExplosionRadius;
			def->iExplosionRadiusMin = asset->iExplosionRadiusMin;
			def->iExplosionInnerDamage = asset->iExplosionInnerDamage;
			def->iExplosionOuterDamage = asset->iExplosionOuterDamage;
			def->damageConeAngle = asset->damageConeAngle;
			def->iProjectileSpeed = asset->iProjectileSpeed;
			def->iProjectileSpeedUp = asset->iProjectileSpeedUp;
			def->iProjectileSpeedForward = asset->iProjectileSpeedForward;
			def->iProjectileActivateDist = asset->iProjectileActivateDist;
			def->projLifetime = asset->projLifetime;
			def->timeToAccelerate = asset->timeToAccelerate;
			def->projectileCurvature = asset->projectileCurvature;
			def->projectileModel = reinterpret_cast<IW5::XModel*>(asset->projectileModel);
			def->projExplosion = static_cast<int>(asset->projExplosion);
			def->projExplosionEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->projExplosionEffect);
			def->projExplosionEffectForceNormalUp = asset->projExplosionEffectForceNormalUp != 0;
			def->projDudEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->projDudEffect);
			def->projExplosionSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->projExplosionSound.sound);
			def->projDudSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->projDudSound.sound);
			def->bProjImpactExplode = asset->bProjImpactExplode != 0;
			def->stickiness = static_cast<IW5::WeapStickinessType>(asset->stickiness);
			def->hasDetonator = asset->hasDetonator != 0;
			def->timedDetonation = asset->timedDetonation != 0;
			def->rotate = asset->rotate != 0;
			def->holdButtonToThrow = asset->holdButtonToThrow != 0;
			def->freezeMovementWhenFiring = asset->freezeMovementWhenFiring != 0;
			def->lowAmmoWarningThreshold = asset->lowAmmoWarningThreshold;
			def->projTrailEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->projTrailEffect);
			memcpy(def->vProjectileColor, asset->vProjectileColor, sizeof def->vProjectileColor);
			def->guidedMissileType = static_cast<IW5::guidedMissileType_t>(asset->guidedMissileType);
			def->maxSteeringAccel = asset->maxSteeringAccel;
			def->projIgnitionDelay = asset->projIgnitionDelay;
			def->projIgnitionEffect = reinterpret_cast<IW5::FxEffectDef*>(asset->projIgnitionEffect);
			def->projIgnitionSound = reinterpret_cast<IW5::snd_alias_list_t*>(asset->projIgnitionSound.sound);
			def->fAdsAimPitch = asset->fAdsAimPitch;
			def->fAdsCrosshairInFrac = asset->fAdsCrosshairInFrac;
			def->fAdsCrosshairOutFrac = asset->fAdsCrosshairOutFrac;
			def->adsGunKickReducedKickBullets = asset->adsGunKickReducedKickBullets;
			def->adsGunKickReducedKickPercent = asset->adsGunKickReducedKickPercent;
			def->fAdsGunKickPitchMin = asset->fAdsGunKickPitchMin;
			def->fAdsGunKickPitchMax = asset->fAdsGunKickPitchMax;
			def->fAdsGunKickYawMin = asset->fAdsGunKickYawMin;
			def->fAdsGunKickYawMax = asset->fAdsGunKickYawMax;
			def->fAdsGunKickAccel = asset->fAdsGunKickAccel;
			def->fAdsGunKickSpeedMax = asset->fAdsGunKickSpeedMax;
			def->fAdsGunKickSpeedDecay = asset->fAdsGunKickSpeedDecay;
			def->fAdsGunKickStaticDecay = asset->fAdsGunKickStaticDecay;
			def->fAdsViewKickPitchMin = asset->fAdsViewKickPitchMin;
			def->fAdsViewKickPitchMax = asset->fAdsViewKickPitchMax;
			def->fAdsViewKickYawMin = asset->fAdsViewKickYawMin;
			def->fAdsViewKickYawMax = asset->fAdsViewKickYawMax;
			complete->fAdsViewKickCenterSpeed = asset->fAdsViewKickCenterSpeed;
			def->fAdsViewScatterMin = asset->fAdsViewScatterMin;
			def->fAdsViewScatterMax = asset->fAdsViewScatterMax;
			def->fAdsSpread = asset->fAdsSpread;
			def->hipGunKickReducedKickBullets = asset->hipGunKickReducedKickBullets;
			def->hipGunKickReducedKickPercent = asset->hipGunKickReducedKickPercent;
			def->fHipGunKickPitchMin = asset->fHipGunKickPitchMin;
			def->fHipGunKickPitchMax = asset->fHipGunKickPitchMax;
			def->fHipGunKickYawMin = asset->fHipGunKickYawMin;
			def->fHipGunKickYawMax = asset->fHipGunKickYawMax;
			def->fHipGunKickAccel = asset->fHipGunKickAccel;
			def->fHipGunKickSpeedMax = asset->fHipGunKickSpeedMax;
			def->fHipGunKickSpeedDecay = asset->fHipGunKickSpeedDecay;
			def->fHipGunKickStaticDecay = asset->fHipGunKickStaticDecay;
			def->fHipViewKickPitchMin = asset->fHipViewKickPitchMin;
			def->fHipViewKickPitchMax = asset->fHipViewKickPitchMax;
			def->fHipViewKickYawMin = asset->fHipViewKickYawMin;
			def->fHipViewKickYawMax = asset->fHipViewKickYawMax;
			complete->fHipViewKickCenterSpeed = asset->fHipViewKickCenterSpeed;
			def->fHipViewScatterMin = asset->fHipViewScatterMin;
			def->fHipViewScatterMax = asset->fHipViewScatterMax;
			def->fightDist = asset->fightDist;
			def->maxDist = asset->maxDist;
			def->iPositionReloadTransTime = asset->iPositionReloadTransTime;
			def->leftArc = asset->leftArc;
			def->rightArc = asset->rightArc;
			def->topArc = asset->topArc;
			def->bottomArc = asset->bottomArc;
			def->accuracy = asset->accuracy;
			def->aiSpread = asset->aiSpread;
			def->playerSpread = asset->playerSpread;
			memcpy(def->minTurnSpeed, asset->minTurnSpeed, sizeof def->minTurnSpeed);
			memcpy(def->maxTurnSpeed, asset->maxTurnSpeed, sizeof def->maxTurnSpeed);
			def->pitchConvergenceTime = asset->pitchConvergenceTime;
			def->yawConvergenceTime = asset->yawConvergenceTime;
			def->suppressTime = asset->suppressTime;
			def->maxRange = asset->maxRange;
			def->fAnimHorRotateInc = asset->fAnimHorRotateInc;
			def->fPlayerPositionDist = asset->fPlayerPositionDist;
			def->szUseHintString = reinterpret_cast<const char*>(asset->szUseHintString);
			def->dropHintString = reinterpret_cast<const char*>(asset->dropHintString);
			def->iUseHintStringIndex = asset->iUseHintStringIndex;
			def->dropHintStringIndex = asset->dropHintStringIndex;
			def->horizViewJitter = asset->horizViewJitter;
			def->vertViewJitter = asset->vertViewJitter;
			def->szScript = reinterpret_cast<const char*>(asset->szScript);
			memcpy(def->fOOPosAnimLength, asset->fOOPosAnimLength, sizeof def->fOOPosAnimLength);
			def->minDamage = asset->minDamage;
			def->minPlayerDamage = asset->minPlayerDamage;
			def->fMaxDamageRange = asset->fMaxDamageRange;
			def->fMinDamageRange = asset->fMinDamageRange;
			def->destabilizationRateTime = asset->destabilizationRateTime;
			def->destabilizationCurvatureMax = asset->destabilizationCurvatureMax;
			def->destabilizeDistance = asset->destabilizeDistance;
			def->fireRumble = reinterpret_cast<const char*>(asset->fireRumble);
			def->meleeImpactRumble = reinterpret_cast<const char*>(asset->meleeImpactRumble);
			complete->adsDofStart = asset->adsDofStart;
			complete->adsDofEnd = asset->adsDofEnd;

			def->worldModel = mem->Alloc<IW5::XModel*>(16);
			for (auto i = 0; i < 16; i++) def->worldModel[i] = reinterpret_cast<IW5::XModel*>(asset->worldModel[i]);

			def->parallelBounce = mem->Alloc<float>(31);
			memcpy(def->parallelBounce, asset->parallelBounce, sizeof(float) * 31);

			def->perpendicularBounce = mem->Alloc<float>(31);
			memcpy(def->perpendicularBounce, asset->perpendicularBounce, sizeof(float) * 31);

			def->locationDamageMultipliers = mem->Alloc<float>(20);
			memcpy(def->locationDamageMultipliers, asset->locationDamageMultipliers, sizeof(float) * 20);

			if (!def->szOverlayName) def->szOverlayName = "";
			if (!def->szModeName) def->szModeName = "";
			if (!def->szAmmoName) def->szAmmoName = "";
			if (!def->szClipName) def->szClipName = "";
			if (!def->szSharedAmmoCapName) def->szSharedAmmoCapName = "";
			if (!def->szUseHintString) def->szUseHintString = "";
			if (!def->dropHintString) def->dropHintString = "";
			if (!def->szScript) def->szScript = "";
			if (!def->fireRumble) def->fireRumble = "";
			if (!def->meleeImpactRumble) def->meleeImpactRumble = "";
			if (!def->turretBarrelSpinRumble) def->turretBarrelSpinRumble = "";
			if (!complete->szInternalName) complete->szInternalName = "";
			if (!complete->szDisplayName) complete->szDisplayName = "";
			if (!complete->szAltWeaponName) complete->szAltWeaponName = "";

			IW5::IWeaponDef::dump(complete, SL_ConvertToString);
		}
	}
}
